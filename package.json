{
  "name": "@r-tui/txiki",
  "version": "0.1.0",
  "description": "@r-tui/txiki",
  "main": "dist/index.js",
  "types": "dist/index.d.js",
  "scripts": {
    "build": "esbuild ./src/index.tsx --bundle --outdir=dist --platform=node --format=esm --minify --define:process.env.NODE_ENV='\"production\"' --sourcemap",
    "dev": "esbuild ./src/index.tsx --bundle --outdir=dist --platform=node --format=esm --minify --define:process.env.NODE_ENV='\"production\"' --sourcemap -w"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@r-tui/ui": "0.1.6",
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@txikijs/types": "^24.6.0",
    "@types/react": "^18.3.3",
    "esbuild": "^0.23.0"
  }
}
